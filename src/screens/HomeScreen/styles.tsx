import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20
    },
    inputContainer: {
        padding: 5,
        height: 150,
        borderWidth: 1,
        borderRadius: 5,
    }
});

export default styles;